#include <stdio.h>
#define TX_FACTOR 100

int main(void)
{
	//	int height;
	//	printf("Enter the height of the box: ");
	//	scanf("%d", &height);
	//	printf("height: %d\n", height * TX_FACTOR);
	
	int d = 33;
	for (int i = 0; i < 100; i++)
	{
		printf("\a");
		return 0;
	}
}
