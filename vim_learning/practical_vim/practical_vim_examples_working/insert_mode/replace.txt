#this file is just about using replace; can delete the . insert ',' and make the B lowercase by overwriting it 
Typing in Insert mode extends the line, but in Replace mode
the line length doesn't change.
